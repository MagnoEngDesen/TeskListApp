/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TeskList from './src/screens/TeskList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TeskList);
